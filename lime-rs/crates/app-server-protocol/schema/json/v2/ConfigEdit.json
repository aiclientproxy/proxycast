{
  "$defs": {
    "MergeStrategy": {
      "enum": [
        "replace",
        "upsert"
      ],
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "keyPath": {
      "type": "string"
    },
    "mergeStrategy": {
      "$ref": "#/$defs/MergeStrategy"
    },
    "value": true
  },
  "required": [
    "keyPath",
    "mergeStrategy",
    "value"
  ],
  "title": "ConfigEdit",
  "type": "object"
}
